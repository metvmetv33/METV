name: YouTube Canlı Yayın Güncelle

on:
  # Her 30 dakikada bir çalış
  schedule:
    - cron: '*/30 * * * *'

  # Manuel tetikleme
  workflow_dispatch:

  # youtube.json değiştiğinde
  push:
    paths:
      - 'youtube.json'

jobs:
  update:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: 📥 Repo'yu klonla
        uses: actions/checkout@v4

      - name: 🐍 Python kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📺 YouTube canlı yayınları güncelle
        run: python scripts/update_youtube.py

      - name: 💾 Değişiklikleri kaydet
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add youtube.json
          git diff --quiet && git diff --staged --quiet || (
            git commit -m "🔄 YouTube canlı yayınlar güncellendi [$(date +'%Y-%m-%d %H:%M')]"
            git push
          )
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
